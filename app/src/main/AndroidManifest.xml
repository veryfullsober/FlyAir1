<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flyair">

    <application
        android:name=".AdSampleApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="FlyAir"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlyAir">
        <activity android:name="com.example.flyair.GameActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.FlyAir.noActionBar"/>
        <activity
            android:name="com.example.flyair.MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.FlyAir.noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>